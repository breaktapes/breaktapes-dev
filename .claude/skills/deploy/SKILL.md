# Ship to Production
1. Run all builds and verify no errors
2. Push current branch and create PR if needed
3. Merge PR to staging, verify deployment
4. Merge staging to main, verify production deployment
5. Report final status with URLs
